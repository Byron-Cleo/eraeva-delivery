# load — Load a feature spec

Reads a spec file from `context/features/` or an inline description and populates `context/current-feature.md` with the feature name, goals, and notes.

Usage: `load <spec-name>` or `load <inline description>`
